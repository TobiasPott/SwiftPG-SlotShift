import SwiftUI

@main
struct MyApp: App {

    // ToDo: add game cycle function called on every turn and used to trigger 'events'
    var body: some Scene {
        WindowGroup(content: { AppMainView() })
    }
}
